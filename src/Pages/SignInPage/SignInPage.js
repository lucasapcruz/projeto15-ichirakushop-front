import { Link } from "react-router-dom";

export default function SignInPage(params) {
    return(
        <>
            <Link to="/sign-up">
                <p>ir para sign-up</p>
            </Link>
        </>
    )
};
